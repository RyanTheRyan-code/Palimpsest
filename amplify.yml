version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd palimpsest-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: palimpsest-app/build
    files:
      - '**/*'
  cache:
    paths:
      - palimpsest-app/node_modules/**/*
